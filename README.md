# WebAuthn Demo

Showcasing WebAuthn passkey as second factor.

Start the server:

```bash
npx tsx server/index.ts
```

Start the client:

```bash
npm run dev
```

First factor:\
Username: `test`\
Password: `test`
